# GPU 환경용 의존성 (VM RTX + CUDA)
# 사용: pip install -r requirements-gpu.txt
# 또는: pip install -e ".[ml]" && pip install torch --index-url https://download.pytorch.org/whl/cu121

# PyTorch with CUDA 12.1 (RTX GPU)
--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0

# ML 의존성 (pyproject.toml [ml]과 동일)
scikit-learn>=1.3.0

# motionanalyzer는 별도 설치: pip install -e .
# 전체 설치 예: pip install -e ".[ml]" && pip install torch --index-url https://download.pytorch.org/whl/cu121
